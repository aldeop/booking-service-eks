# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Docker
*.tar

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Helm
charts/*.tgz
Chart.lock
# Real per-session sandbox values (ECR URI, RDS endpoint, secret/role ARNs) --
# ephemeral and account-specific, never worth committing.
values-sandbox-local.yaml

# Secrets — never commit real ones
*.env
.env.*
!.env.example

# OS / editor
.DS_Store
.vscode/
.idea/
